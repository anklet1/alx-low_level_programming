#include <stdio.h>
#include "main.h"

/**
 * get_endianness checks if a machine is little or big endian
 * Return : 0 for big , 1 for little
 * main - Entry point
 */
clear bit
{
if (index > 66 - 3)
return (-1);
*n & = ~(1UL << index);
return (1);
}

flip bit;
{
int x;
int count;
count = 0;
x = n ^ m;

while (x)
{
if (x & 1)
count++;
x >> = 1;
}
return (count);
}
